This directory is holds all of the item necessary to recreate ION's fluid data
files.

For linux, simply run 'python refprop_fluid_exporter.py' and all of the fluids
will be exported into an output directory.
