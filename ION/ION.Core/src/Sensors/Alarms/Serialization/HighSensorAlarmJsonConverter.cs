using System;

using Newtonsoft.Json;

namespace ION.Core.Sensors.Alarms.Serialization {
/*
  public class HighSensorAlarmJsonConverter : JsonConverter {

    // Overridden from JsonConverter
    public override bool CanConvert(Type objectType) {
      return typeof(HighSensorAlarmJsonConverter) == objectType;
    }

    public override void WriteJson(JsonWriter writer, object value, JsonSerializer serializer) {
//      var ion = AppState.context;
//      var model = serializer.Deserialize(reader) as Ab
    }

    internal class HighSensorAlarmModel { 
    }
  }
*/
}

