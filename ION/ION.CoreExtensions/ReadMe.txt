
This "project" is really a super level glue between the ION.Core PCL and the ION front
end (Android, iOS). It provides a number APIs that are not available in the core's PCL
framework but are shared across all front end platforms.