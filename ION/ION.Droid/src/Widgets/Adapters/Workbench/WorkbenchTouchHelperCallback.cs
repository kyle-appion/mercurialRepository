namespace ION.Droid.Widgets.Adapters.Workbench {

  using System;

  using Android.Support.V7.Widget;
  using Android.Support.V7.Widget.Helper;

/*
  /// <summary>
  /// An implementation of an ItemTouchHelper.Callback that will manager the proper delegate and routing of 
  /// item touch events for a recycler view.
  /// </summary>
  public class WorkbenchTouchHelperCallback : ItemTouchHelper.Callback {

    /// <summary>
    /// Whether or not draging a view is allowed by the callback.
    /// </summary>
    public bool allowDrag;
    /// <summary>
    /// Whether or not swiping a view is allowed by the callback.
    /// </summary>
    public bool allowSwipe;

    public WorkbenchTouchHelperCallback() {
    }

    /// <summary>
    /// Determines whether this instance can drop over the specified recyclerView current target.
    /// </summary>
    /// <returns><c>true</c> if this instance can drop over the specified recyclerView current target; otherwise, <c>false</c>.</returns>
    /// <param name="recyclerView">Recycler view.</param>
    /// <param name="current">Current.</param>
    /// <param name="target">Target.</param>
    public override bool CanDropOver(RecyclerView recyclerView, RecyclerView.ViewHolder current, RecyclerView.ViewHolder target) {
      if (current is ManifoldViewHolder && target is ManifoldViewHolder) {
        return true;
      } else {
        return false;
      }
    }
  }

*/
}

