namespace ION.Droid.Rss {

  using System;

  using Android.App;
  using Android.Content;
  using Android.OS;
  
	public class RssHelper {
/*
    public static PendingIntent CreateRssAlarmIntent(Context context) {
      var am = context.GetSystemService(Context.AlarmService) as AlarmManager;

      var pi = 

      am.SetInexactRepeating(AlarmType.ElapsedRealtime, 0, AlarmManager.IntervalDay, pi);
    }
*/
  }
}
